configuramos o git na maquina com os seguntes comenados:

git config --global user.name "<name>"
git config --global user.email "<email>"

configuramos a chave ssh

Salvar arquivo = cntrl + s
Atalho para abrir pasta = cntrl + k + o

Canal professor Renan: INIT CODE

Em java scrips precisa usar aspas quando é texto

texto = string
juntar texto = concatenar
exemplo: ("1"+"1")
= é = a atribuiçaos
nome de arquivo,pasta NUNCA colocar assentos